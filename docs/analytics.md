# 使用统计

SullyOS 官方部署（`qegj567-cloud.github.io` 和 `sully-os-nu.vercel.app`）接了一套自托管
[umami](https://umami.is/)，用来数「哪个页面被打开了、哪个功能被用了一次」。

这份文档说清楚三件事：**收什么、怎么关、怎么自己核实**。改任何统计相关的代码前先读完。

## 一句话版本

不用 cookie、不存 IP，永远不碰你和角色的任何对话、记忆、设定。只数计数。

不叫它「匿名统计」，是因为严格说这不算匿名——[往下看](#这是假名不是匿名)。

## 代码在哪

| 东西 | 位置 |
|---|---|
| 唯一的上报出口、开关、档位换算 | [`utils/analytics.ts`](../utils/analytics.ts) |
| 启动时决定要不要挂统计脚本 | [`index.tsx`](../index.tsx) |
| 「打开了哪个 App」+ 数据规模 | [`context/OSContext.tsx`](../context/OSContext.tsx) |
| 设置页的开关 | [`apps/Settings.tsx`](../apps/Settings.tsx) |
| 回归测试 | [`utils/analytics.test.ts`](../utils/analytics.test.ts) |

**全仓库只有一个出站口**：`utils/analytics.ts` 里的 `trackEvent()`，它内部是
`window.umami?.track(...)`。所有埋点都调 `trackEvent`，没有第二条路。想知道到底会发出什么，
读这一个文件加上全仓库对 `trackEvent` 的调用就够了。

这么设计不只是为了好读——调用点没法绕过开关判定，关掉之后不可能有漏网的。

## 数据发到哪

自托管的 umami 实例：`https://stats.palm.cdsv.cc`（站点 id `3f775277-882d-4453-be2c-3226eddab438`）。
后台只有维护者能进，不接任何外部服务。

这两个值**不写在源码里**，由构建环境注入：

| 部署 | 怎么配 |
|---|---|
| GitHub Pages | 仓库 Variables 里的 `UMAMI_SCRIPT_URL` / `UMAMI_WEBSITE_ID`，由 `.github/workflows/deploy-pages.yml` 传给构建 |
| Vercel | 项目环境变量 `VITE_UMAMI_SCRIPT_URL` / `VITE_UMAMI_WEBSITE_ID` |

之所以不硬编码：写死在源码或工作流里的话，任何人 fork 过去一构建，他的用户就会朝这个地址发请求
（`data-domains` 会让 umami 丢弃这些数据，但请求本身已经从别人的浏览器发出去了）。
放进 Variables 之后 fork 拿不到值，构建出来天然是零统计的版本。

## 什么情况下一个请求都不发

`initAnalytics()` 里四道早退，任意一条中了就直接返回，**连 script 标签都不创建**：

1. **构建时没配环境变量**。`VITE_UMAMI_SCRIPT_URL` 和 `VITE_UMAMI_WEBSITE_ID` 两个都配齐才会加载。
   官方部署在构建环境里配，自部署默认没有——所以你自己部署的实例是零统计的，不用做任何事。
2. **用户在设置里关掉了**。
3. **浏览器开了 Do Not Track**。不用动开关，自动跳过。
4. 已经挂过一次了（防重复）。

tracker 标签上还挂了 `data-do-not-track="true"` 和 `data-domains`（只认那两个官方域名），
作为第二道保险——万一以后加载路径改了，这层不会跟着一起失效。

另外刻意关掉了 umami 的自动上报（`data-auto-track="false"`），页面访问由我们自己显式发一次。
这样「所有出站请求都经过 `trackEvent`」才是字面成立的，审计时不必去推断 tracker 会不会背着我们多发。

## 本地存了什么

只有 `localStorage` 里一个 `os_analytics` 键，内容是 `{"enabled": true/false}`——
**开关本身的状态**，跟其他设置（`os_theme`、`os_api_config`）一样是你自己的偏好。

统计**不给自己记账**：「数据规模每会话最多报一次」的节流标记只是个内存变量，
标签页一关就没了，不落 localStorage、不落 IndexedDB、不落 cookie。
（这条有测试钉着，见 `analytics.test.ts` 里「统计不给自己记账」那条。）

## 收集频率 vs 统计周期

客户端只管**每次会话丢一票**，不含任何周期假设。想看月度分布就在 dashboard 里把日期范围框成
一个月，想看周报就框一周——周期住在查询侧，客户端一行代码都不用动。

正因为不含周期假设，它才兼容一切周期。反过来把「每月一次」做进客户端，就得在浏览器里
记「上个月报过了」，那才是真往用户设备上写追踪状态。

顺带说一下口径：每会话采 = **会话加权**，重度用户票多。这对多数决策反而是对的权重
（「有多大比例的会话扛着 500+ 条记忆」问的就是压力，不是人头占比）。要按人头的口径，
查询时按独立访客去重即可，库在自己手里。

## 永远不会收集

- 你和角色的任何对话内容、记忆、角色设定、你的设定
- 你在任何地方输入的文字、表单内容
- API 配置、MCP 配置等任何设置值（哪怕只是「有没有填」这种布尔也不碰）
- cookie；IP 不落库（反代的 access log 也关掉了 IP）
- 跨站追踪、录屏、热力图（umami 自带这几样，我们不开）

## 这是假名，不是匿名

这两个词在数据保护里有确定的分工，差别不是修辞：

| | 意思 | 后果 |
|---|---|---|
| **匿名**（anonymous） | 处理后**再也**关联不到任何个人，不可逆 | 已经不算个人数据，GDPR 这类法规不再适用 |
| **假名**（pseudonymous） | 单看数据认不出是谁，但配合其他信息仍能关联到同一个人 | **仍然算个人数据** |

**这套统计属于后者。**

`当前外观` 和 `当前角色设置` 加起来有 60 多个维度。单看每一个都是无害的枚举
（头像是圆的还是方的、气泡用哪套样式），但**这么多维度组合起来，对多数人是唯一的**。
配上 umami 自带的地区、浏览器、屏幕尺寸，技术上可以把库里两条记录判定成同一个人——
哪怕 umami 的访客哈希每天轮换。

所以这两句话要分开说：

- **收集的内容里没有身份信息**：没有账号、没有 cookie、没有 IP 入库，没有任何一个字段
  来自你的对话、记忆、设定或输入。
- **但「无法关联到个人」不成立**：技术上做得到。我们不做，也没有任何代码在做，
  但这是靠承诺和可审计性保证的，不是靠技术上不可能。

所以这份文档和界面上都写「使用统计」，不写「匿名统计」——后者是个兑现不了的说法。

验证方式跟其他部分一样：代码全公开，数据库只有维护者能进，所有涉及数据的改动只能走公开 PR。
发现不对随时开 issue。

## 怎么自己核实

1. **读源码**。上面那张表，加上全仓库 grep `trackEvent`，就是全部。
2. **按 F12**，打开 Network 面板。前端跑在你自己的设备上，每个请求装了什么藏不住。
3. **看 PR**。所有涉及数据的改动只能走公开 PR。

## 加新埋点的规矩

1. 事件名用中文，跟用户在界面上看到的说法一致——按 F12 的人不用再对照一份英文 slug 表。
2. 属性**只能是固定枚举**，取值集合必须写死在代码里。任何来自用户输入或模型输出的字符串
   一律不许进去。
3. 遇到 dispatch 型的分发点（一处覆盖十几个子功能），**必须写显式白名单**。
   默认放行等于给未来挖坑：以后谁加一个新分支就自动被上报了。
4. 改完在这份文档的事件清单里补一行。

## 完整事件清单

下面是**代码里实际存在的全部事件**（从源码 grep 出来的，不是照计划抄的）。
每一条只发事件名和固定枚举属性。

### 页面访问

- 页面被打开 —— 来源、浏览器 / 系统 / 设备类型、国家级地区、屏幕尺寸，由 umami 自带

### 数据规模

每次会话最多上报一次，只发区间，不发精确值：

- `数据规模` —— 角色数、记忆条数、单角色最大记忆条数、单角色最大聊天条数、本地存储占用、是否装成 PWA 全屏运行

### 当前在用哪套外观

每次会话最多一次。报的是**现在用的是哪个**，不是**点过哪个**——
后者只有折腾的人会出现，拿来决定砍哪个预设会砍反。
用户自己捏的主题一律记 `custom`，绝不带他起的名字。

- `当前外观` —— 桌面皮肤 / 桌面版本 / 深色模式 / 隐藏状态栏 / 图标轮廓 / 播放卡片浅色 /
  动森聊天同步 / 自定义字体(用没用) / 气泡主题 / 气泡样式 / 聊天壳样式 / 顶栏样式 /
  输入栏样式 / 聊天背景 / 消息间距 / 时间戳显示 / 顶栏对齐 / 顶栏密度 / 状态样式 /
  发送键样式 / 卡片对齐 / 头像形状 / 头像尺寸 / 头像显示 / 头像对齐 / 头像模式 /
  表情尺寸 / 隐藏侧贴边 / 准备中圆点 / 隐藏情绪栏 / 自定义白框CSS(用没用) /
  提示音 / 气泡字号·行距·缩进·头像微调(只报调没调过)

- `当前角色设置` —— 开关类报「有没有人开过」或「有没有人特意关掉」（按默认值决定问法）：
  记忆宫殿 / 自动归档 / 思考过程 / 日程与情绪 / HTML卡片 / 角色级聊天装扮 / 自定义时区 /
  生活记录注入 / 小红书 / 隐藏系统日志 / 见面轻阅读 / 观测协议 / 彼方自主登入 /
  提示音绑白框 / 时间感知 / 见面时间感知 / 查手机同步聊天 / 允许虚构NPC /
  可读我的音乐 / 见面深挖引导。
  选择类报当前活跃角色选的那个：思考链风格 / 日程风格 / 认知风格 / 原文读取策略 /
  见面写作风格 / 见面叙事人称 / 观测HUD样式 / 定时消息模式 / 定时消息频率 / 角色提示音。

用户自己填的（提示音直链、上传的音频、自定义字体、白框 CSS）一律只报 `custom` 或
「用了 / 没用」，原文一个字都不出去。开放数值（字号、行距、缩进、偏移）只报「调过 / 默认」。

**没报的**：每个世界一份（家园）、每局一份（跑团）的那些设置。一个用户能有十几个世界，
报哪个都不代表他；这些选择本来就有「选择世界时间模式」这类事件在记。

### 本次会话聊了多少

**发消息本身不打点**。那样服务端会攒出带时间戳的发送流水，
哪怕没有内容，「谁在几点连着发了多少条」也还原得出来。
这里只在页面切到后台时报一次区间，档位没变就不再报；一条没发过的会话一个点都不产生。

- `私聊会话发送条数` —— 0 / 1-10 / 11-50 / 51-200 / 200+

### 打开某个 App

由 `context/OSContext.tsx` 单点统一发，桌面上每个 App 各一条，形如 `打开见面`、`打开记忆宫殿`。名字取自 `constants.tsx` 里的 App 列表：

- 神经链接 / 记忆宫殿 / Message / 电话 / 群聊 / 小小窝 / 查手机 / 见面 / 档案 /
  存钱罐 / 交换日记 / Spark / 自习室 / TRPG / 笔友会 / 写歌 / 彼方 / 时光契约 /
  世界书 / 热点 / 使用帮助 / 相册 / 自由活动 / 小红书图库 / 气泡工坊 / 外观 /
  设置 / 攻略本 / 都市人生 / 特别时光 / 音乐 / 捏脸·开发 / 家园

### 各功能入口与卡住信号

**设置**

- 从云端恢复失败
- 代理地址格式被拒
- 保存备份文件到手机失败
- 关闭主动消息 Push 加速
- 关闭云端备份
- 关闭原生工具调用（退回文字兼容模式）
- 切换主动消息Push加速
- 切换云端备份服务商
- 刷新 Web Push 诊断
- 加载云端备份列表
- 发一条测试推送
- 发送测试推送（主动消息加速）
- 取消导出前的密钥确认
- 启用 Push 加速器的结果
- 启用主动消息 Push 加速
- 启用未测通的 MCP 服务器被拦下
- 在 Push 加速启用确认弹窗做出选择
- 复制 MCP 部署指引给 AI
- 复制 cookie 教程文本
- 导入备份失败
- 导出备份失败
- 导出本地备份
- 展开 GitHub 高级选项
- 展开获取 cookie 教程
- 恢复默认代理 Worker
- 打开Instant Push配置
- 打开MCP工具服务器配置
- 打开「MCP 是什么」说明弹窗
- 打开实时感知配置
- 测试 MCP 服务器连接
- 测试 Notion 连接
- 测试 WebDAV 连接
- 测试天气数据源连接
- 测试小红书桥接连接
- 测试并连接 GitHub
- 测试点单 MCP 连接
- 测试飞书连接
- 深度重置推送订阅
- 跳去 GitHub 创建 Token
- 跳转 MCP 完整教程
- 连接云端备份服务商
- 重置推送订阅

**系统级组件**

- App 加载卡死超时
- 一键把聊天存进记忆宫殿
- 上传备份到云端
- 从卡死页返回桌面
- 切换调试日志录制
- 勾选调试日志类别
- 卡死页点刷新恢复
- 处理收到的转账
- 处理角色代记的生活记录
- 复制最新 Worker 代码
- 导入恢复提醒选稍后再说
- 导出调试日志
- 弹出 Worker 更新提醒
- 弹出上次导入未完成提醒
- 弹出导入中断恢复提醒
- 弹出报错详情弹窗
- 弹出版本更新提醒
- 弹出该备份啦提醒
- 强制关闭调试面板
- 打开 Worker 控制台
- 打开调试面板
- 批量删除选中的消息
- 把瑞幸购物车发给角色
- 把语音条转成文字
- 把麦当劳购物车发给角色
- 探测到 Worker 版本漂移
- 播放语音条
- 标记 Worker 已部署新版
- 清空瑞幸购物车
- 清空麦当劳购物车
- 点去重新导入
- 点知道了稍后再说
- 点稍后再说放着不管
- 点立即备份
- 稍后处理 Worker 更新
- 跳去看更新说明
- 跳过本次更新说明
- 转发选中的消息
- 重测社区迁移检测
- 重置调试面板
- 长按复制心象全文

**音乐**

- 下载生成的歌到本地文件
- 从本地专辑移除一首歌
- 保存歌词对轴
- 做一次网易云每日签到
- 切换我的云音乐标签
- 切换播放模式
- 切换音质档位
- 切歌（上一首/下一首）
- 删除角色歌单里选中的歌
- 手动粘贴 Cookie 登录网易云
- 打开播放页
- 打开歌词对轴面板
- 搜索一首歌
- 播放「我的」页列表里的一首歌
- 播放搜索结果里的一首歌
- 播放每日推荐
- 播放私人 FM
- 播放角色歌单里的一首歌
- 收藏或取消收藏当前歌
- 生成角色的音乐人格
- 用手机号验证码登录网易云
- 结束和角色的一起听
- 让角色按品味挑歌填满歌单
- 运行音乐服务诊断
- 进入角色音乐角落
- 退出网易云登录
- 重新生成角色的音乐人格

**设置 · 配置弹窗**

- 发送 Instant Push 测试推送
- 复制 Deno Loader
- 复制 Instant Push Worker 代码
- 复制 VAPID 公钥
- 复制 VAPID 私钥
- 复制 Worker env 清单
- 对比已部署 Worker 版本
- 展开单条输入构成
- 开启通知与推送订阅
- 打开 Cloudflare Dashboard
- 打开 Deno 控制台
- 打开 Instant Push 视频教程
- 打开实际后端字段说明
- 查询 Service Worker 版本
- 检查用户密钥
- 检测 Instant Push Worker 连接
- 清空 API 调用记录
- 清空 VAPID 密钥对
- 点击复制失败兜底的 GitHub bundle 链接
- 生成 VAPID 密钥对
- 记录里出现模型不符警告
- 跳去配置推送凭据 (VAPID)
- 连接并启用主动消息 2.0
- 连点版本号解锁调试面板

**像素家园**

- 保存捏好的像素形象
- 切换仓库与工坊分区
- 切换像素房间装修模式
- 删除一件像素家具
- 导入像素家园预设
- 导出像素家园预设
- 导出像素素材包
- 打开像素捏人器
- 打开像素资产仓库
- 批量删除像素素材
- 换上自定义墙纸或地砖
- 摆放一件像素家具
- 清空像素房间的家具
- 生成一批像素素材
- 直接导入现成像素素材
- 给像素素材加标签
- 进入像素家园房间
- 进入记忆潜行模式

**见面**

- 删除一条见面消息
- 删除见面记录里的一条消息
- 恢复上次见面进度
- 打开见面记录
- 打开见面设置面板
- 批量删除见面消息
- 检出见面存档崩溃并清理
- 编辑一条见面消息
- 编辑见面记录里的一条消息
- 见面存档选重新开始
- 走过去开始见面会话
- 进入见面感知页
- 选择见面存档处理方式
- 重掷见面回复
- 重新感知一次角色状态

**Message（私聊）**

- 下载语音条
- 删除一条消息
- 启动主动消息
- 复制一条消息
- 开启瑞一杯聊天点单
- 开启聊天翻译
- 引用回复一条消息
- 归档聊天记录
- 打开日程小剧场
- 打开聊天功能面板项
- 打开麦当劳点单小程序
- 清空聊天记录
- 编辑一条消息
- 重新生成回复

**写歌**

- 分享乐谱到聊天
- 切换出歌生成器
- 完成乐谱并让搭档点评
- 开始新建乐谱
- 打开 AI 出歌引导弹窗
- 找搭档讨论接下来怎么写
- 把成品歌加进音乐 App 专辑
- 确认出歌并开始生成
- 让 AI 帮写出歌提示词
- 请搭档给灵感
- 进入乐谱写作模式
- 选择加入音乐 App 的封面样式
- 选择声线预设
- 采纳搭档写的一句歌词

**家园**

- 切换角色手机分区
- 打开角色手机面板
- 新建家园世界
- 点燃一条伏笔
- 用 AI roll 出 NPC
- 给角色传一句心声
- 编辑角色手机里的内容
- 补发观测到聊天
- 触发一轮世界观测
- 转发角色动态到聊天
- 进入家园世界
- 选择世界存在感模式
- 选择世界时间模式
- 重演某个角色这一段

**小小窝**

- 切换小小窝分区
- 切换生活碎片面板
- 复制选中的家具
- 套用内置样板房
- 导入小屋样板房
- 导出小屋样板房
- 戳一戳角色
- 打开家具超市
- 打开梦境剧场
- 批量导入家具素材
- 摆放一件家具
- 生成今天的房间
- 观察房间里的家具
- 进入房间装修模式

**彼方**

- 一键发送待发的回信
- 一键寄出漂流信
- 上架一本小说到书库
- 切换彼方顶部标签
- 切换邮局信件分类
- 复制邮局身份码
- 开启用户本人接入彼方
- 开启角色接入彼方
- 打开小说上架弹窗
- 指定角色去邮局回这封来信
- 播放听歌房正在放的歌
- 清空彼方留言墙
- 给陌生来信点赞或举报
- 进入彼方房间

**攻略本**

- 从某回合重新生成
- 删掉某回合之后的内容
- 删除一份攻略存档
- 回放历史攻略存档
- 开始一局攻略
- 手动编辑一个选项
- 手动编辑本回合场景
- 打开玩法说明
- 把结算卡发到聊天
- 结束本局出结算卡
- 让 AI 帮写本回合选项
- 选择幻想场景预设
- 选择攻略回合数
- 选择攻略角色进入配置页

**查手机**

- 偷看 AI 助手使用记录
- 切换允许虚构 NPC 开关
- 切换智能体服务分类
- 切换酒馆阅读皮肤
- 刷新生成手机 App 数据
- 安装自定义 App
- 手动添加一位联系人
- 打开查手机子应用
- 打开联系人对话详情
- 把归档记录绑定到人际关系
- 清空全部聊天归档
- 生成一段与联系人的对话
- 生成人格模拟演出
- 用角色卡开一局

**神经链接**

- 切换角色详情标签页
- 导入角色卡
- 导出角色卡
- 应用音色到角色
- 开启角色生活记录注入
- 打开QQ捏人工坊
- 打开批量记忆总结弹窗
- 打开挂载世界书弹窗
- 打开角色分组管理弹窗
- 执行批量记忆总结
- 执行记忆导入清洗
- 提炼当月核心记忆
- 生成角色印象
- 给角色挂载世界书

**笔友会**

- 上传书稿封面图
- 保存一个剧中人
- 保存角色创作档案
- 删除一本书稿
- 导入一个剧中人
- 导入一条世界书设定
- 打开书稿写作页
- 打开导入世界书弹窗
- 打开导入剧中人弹窗
- 打开小说设定页
- 打开新建书稿页
- 打开角色库
- 新建一本书稿
- 重置创作档案为自动分析

**群聊**

- 切换群聊回复生成模式
- 切换群聊总结整理方式
- 删除一条群消息
- 复制一条群消息文字
- 开启群聊 HTML 模式
- 引用回复一条群消息
- 手动整理群话题盒
- 打开创建群聊弹窗
- 打开发红包面板
- 打开群设置面板
- 清空群聊记录
- 编辑一条群消息
- 重新生成群聊回复
- 领取或退回群红包

**自习室**

- 交卷让老师批改
- 保存自习室独立 API 线路
- 保存讲课风格预设
- 删除一份试卷
- 删除一门课程
- 学完本章进入下一章
- 对错题追问
- 导入 PDF 教材
- 开始刷题
- 打开章节目录
- 打开练习册
- 打开自习室设置
- 进入课程课堂
- 重新生成本章讲解

**记忆宫殿**

- 同步记忆向量到云端
- 导入记忆宫殿备份
- 导出记忆宫殿备份
- 开启全自动记忆
- 开启记忆宫殿
- 手动关联两条记忆
- 手动触发认知消化
- 打开事件盒列表
- 打开手动区间总结面板
- 打开记忆宫殿房间
- 整理历史记忆到门牌
- 清空全部记忆数据
- 评估角色认知参数
- 运行手动区间总结

**Spark**

- 分享帖子到聊天
- 切换 Spark 主标签
- 切换个人主页子标签
- 删除一条帖子
- 刷新 Spark 推荐流
- 打开分享帖子面板
- 打开发布笔记面板
- 打开身份管理面板
- 收藏一条帖子
- 更换 Spark 头像
- 清空 Spark 推荐流
- 点赞一条帖子
- 给角色添加一个马甲

**TRPG**

- 切换 SAN 值锁定
- 切换骰子判定
- 创建冒险开团
- 删除跑团存档
- 回退剧情到某条记录
- 归档冒险并写进角色记忆
- 恢复默认阅读外观
- 打开存档继续冒险
- 打开跑团系统菜单
- 用 AI 生成世界观
- 转发剧情片段到聊天
- 重新推演上一段剧情
- 重置本局冒险

**交换日记**

- 从日记页撕掉一张贴纸
- 保存日记
- 切换日记纸张样式
- 切换日记页标签
- 删除一个自定义贴纸
- 删除一篇日记
- 导入自定义贴纸
- 归档日记进神经链接
- 往日记页贴一张贴纸
- 打开自定义贴纸导入弹窗
- 打开贴纸面板
- 进入日记书写页
- 邀请角色交换日记

**存钱罐**

- 切换记账 App 底部标签
- 删除一笔账
- 手动刷新店铺情报志
- 打开店铺情报志
- 打开新增心愿弹窗
- 打开玩法说明
- 打开记一笔弹窗
- 新增一个存钱心愿
- 解锁新甜品配方
- 解雇店员
- 让店员休息
- 记一笔账
- 雇一个新店员

**电话**

- 下载一条通话语音
- 修改自己的通话发言
- 再打一通电话
- 切换语音输入
- 删除一条通话记录
- 发起通话
- 打开通话记录
- 挂起通话到后台
- 设置通话语音语种
- 重掷角色的通话台词
- 重播一条通话语音
- 重播通话记录里的语音

**手账**

- 保存手账页编辑
- 切换手账分区
- 切换角色生活流深度
- 打开新建打卡面板
- 打开生成今日面板
- 撕掉一页手账
- 新增手写页
- 新建一个打卡项
- 标记手账页不入册
- 生成今日手账
- 重新生成角色小生活

**都市人生**

- 保存小人设定编辑
- 全选参与角色
- 切换城市面板标签
- 吃瓜围观推进一轮
- 启用独立 API 线路
- 往城市里拉一个小人
- 打开城市设置面板
- 搅动世界（搅局）
- 清空参与角色
- 筛选动态流
- 重置城市

**梦境剧场**

- 打开梦境盲盒收藏册
- 打开梦境规则说明
- 打开梦的残页存档
- 撕掉一页梦的残页
- 无视今日提醒再看一场梦
- 生成一场梦境
- 重看一场存档梦境
- 重看一遍刚做完的梦

**系统兜底**

- 从崩溃页返回桌面
- 冷启动等待数据就绪
- 复制报错信息
- 复制报错详情
- 点刷新重试（资源加载失败）
- 自动刷新恢复资源加载失败
- 触发 App 崩溃兜底页
- 跳过开机动画

**外观**

- 切换外观定制标签页
- 切换桌面整机风格
- 导入外观预设文件
- 应用已保存外观预设
- 应用配色预设
- 打开桌面装饰贴纸库
- 添加桌面装饰贴纸

**气泡工坊**

- 切换气泡编辑对象
- 切换编辑工具分区
- 切换预览场景
- 展开我的气泡作品库
- 应用气泡风格模板
- 插入限定作用域 CSS 片段

**世界书**

- 切换世界书注入位置
- 导出分组为标准世界书
- 打开世界书编辑页
- 打开导入世界书弹窗
- 进入批量管理模式

**人格模拟**

- 切换人格模拟类型
- 切换演出自动播放
- 打开生活记录
- 选择你的存在感
- 选择演出基调

**捏声音**

- 切换捏声音标签页
- 切换试听语种样例
- 打开音色选择器
- 把捏好的声音应用到角色

**捏脸·开发**

- 单张追加部件
- 导出内置素材包
- 批量加入 PSD 部件到捏人器
- 选择 PSD 文件批量导入

**相册**

- 删除一张照片
- 打开角色相册
- 清空一个角色的相册
- 让角色点评这张照片

**时光契约**

- 切换日程标签页
- 切换日程界面主题
- 打开新建条目弹窗

**小红书图库**

- 打开囤图添加表单
- 按标签筛选图库

**工具层兜底**

- 拒绝导入第三方备份
- 触发翻译白屏护栏

**桌面**

- 打开角色日程面板
- 进入桌面整理模式

**自由活动**

- 开始角色自由活动
- 打开自由活动记录详情

**QQ 桥**

- 切换 QQ 桥接开关

**使用帮助**

- 切换常见问题标签页

**档案**

- 切换个人档案标签页

**热点**

- 手动刷新热点日报

共 487 条。
